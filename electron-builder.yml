appId: website.app.electron-vue-vite

productName: electron-vue-vite

appImage:
  artifactName: ${productName}-${version}-${arch}.${ext}

copyright: Copyright © 2025 ${productName} ${author}

buildDependenciesFromSource: true

directories:
  app: app
  output: release/${version}

asarUnpack:
  - '**/node_modules/**'

win:
  publish:
    # an object provider for github with additional options
    - provider: github
      protocol: https
      # a string provider for bitbucket that will use default options

publish:
  provider: github
  owner: Aurora-flower
  repo: Electron-Vue3-Vite
  releaseType: draft
